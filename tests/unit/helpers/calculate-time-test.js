import { calculateTime } from 'craigslist/helpers/calculate-time';
import { module, test } from 'qunit';

module('Unit | Helper | calculate time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calculateTime([42]);
  assert.ok(result);
});
