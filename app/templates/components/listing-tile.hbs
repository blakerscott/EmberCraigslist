{{#each model.listings as |listing|}}
    <div>
      <ul>
        <li>{{listing.image}}</li>
        <li>{{listing.name}}</li>
        <li>{{listing.dateAdded}}</li>
        <li>{{listing.location}}</li>
      </ul>
    </div>
  {{/each}}
